// El conductor de las pruebas de integración. Es el mínimo que pide
// `flutter drive`; toda la lógica está en integration_test/.
import 'package:integration_test/integration_test_driver.dart';

Future<void> main() => integrationDriver();
